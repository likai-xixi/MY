# Session: R-12A Runtime

## Task

`TASK-0012` - execute the approved R-12A product semantics and option-set/value breaking migration.

## Goal

Complete only the approved destructive catalog/option slice from immutable review base `f28e3d12358bdc35ac1782fd50be7850f937bc1b`, including real runtime and rollback evidence, while leaving later engineering and order runtime blocked.

## Status

`done`

## Changed Files

- Exact list: `ai/changes/CR-20260720T134007Z-change/changed-files.json`.
- Java/Mapper masterdata runtime and focused tests.
- Vue masterdata pages/API contract and focused structural tests.
- V007 migration, dedicated/shared validation, SQL ownership.
- Current contracts, registries, scans, graphs, memory, and handover.
- Runtime evidence under `ai/changes/CR-20260720T134007Z-change/runtime-evidence/`.

## Commands

- `npm run resume`; `npm run impact -- masterdata`; review and phase-gate checks.
- `node --test tests/masterdata-runtime.test.js`.
- Maven unit/integration verification and eight-module package.
- `npm run build` in `ruoyi-ui`.
- V007 migration and both MySQL validation SQL files.
- Real API/browser/menu/permission acceptance and rollback rehearsal.
- `npm run scan:all`; `npm run finalize:change`; `npm run check`; `npm run close:change`; `git diff --check`.

## Verification

- [local] Review-only baseline is committed without push and bound as `impact.baseRevision`; review package diff is zero.
- [runtime-local] Strategy A produced 4 option sets, 2 values, and four `SINGLE` modes; old tables/menu/permission aliases, orphans, and duplicates are absent.
- [runtime-local] API/browser acceptance and whole-state old-code rollback rehearsal passed.
- [local] Focused Node 39/39, Java unit 65/65, MySQL integration 2/2, UI 7/7, Maven package, Vue build, and scans passed.
- [local] Full `npm run check` passed with 491/491 Node tests; `npm run close:change` and `git diff --check` passed.
- [local] Forbidden adjacent runtime diff is zero and both phase gates remain blocked.
- [not-run] Implementation commit, push, and CI.

## Risks

- Generated DB scan is lexical over migration history; live validation SQL is the runtime authority for dropped-table absence.
- The implementation is local and uncommitted, so it is not yet immutable or CI-verified.

## Evidence

- `ai/changes/CR-20260720T134007Z-change/runtime-evidence/database-acceptance.md`.
- `ai/changes/CR-20260720T134007Z-change/runtime-evidence/api-browser-acceptance.md`.
- `ai/changes/CR-20260720T134007Z-change/runtime-evidence/build-test-results.md`.
- `engineeringCoreReady=blocked`; `beforeSalesOrder=blocked`.

## Next Entry Point

Wait for explicit user instruction before committing or pushing the implementation. Do not edit the review decision or begin R-12B.
