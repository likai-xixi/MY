# Session: CI-FIX-01 Frontend Dependency Security Repair

## Task

`TASK-0002` - repair the frontend dependency audit blocker independently from R-12A business implementation.

## Goal

Resolve the exact `brace-expansion` advisory through the smallest compatible lockfile change, prove local regression safety, create local commits, and stop before push.

## Status

`in_progress`

## Changed Files

- Exact list: `ai/changes/CR-20260721T125309Z-ci-fix-01-brace-expansion-audit-remediation/changed-files.json` after finalization.
- Implementation surface: `ruoyi-ui/package-lock.json` and `tests/frontend-dependency-hardening.test.js` only.
- Independent review/change, platform context, and resumable memory evidence accompany the implementation.

## Verification

- [local] `GHSA-3jxr-9vmj-r5cp` / `CVE-2026-13149` affects local `brace-expansion@2.1.1`; patched 2.x is `2.1.2` and requires no major.
- [local] Three logical paths share the node: two through used production dependency `js-beautify` and one through dev dependency `unplugin-auto-import`.
- [local] Lockfile-only normal npm resolution reaches `2.1.2`; post-fix clean install and audit report zero vulnerabilities.
- [local] Platform 5/5, UI 7/7, production build, R-12A Node 39/39, Java 65/65, and MySQL integration 2/2 pass.
- [local] Independent review-only commit `6a9bfd57548160d85e28776f415b5883b6d92836` precedes implementation; R-12A review/decision and forbidden runtime diffs are zero.
- [local] Both phase gates remain blocked; R-12B has not started.
- [local] Complete `npm run check` passes every gate with 491/491 root Node tests; explicit `npm run close:change` and `git diff --check` pass.
- [not-run] Push, GitHub Actions, after-push checks, and post-push handover.

## Commands

- [local] `npm --prefix ruoyi-ui ci`
- [local] `npm --prefix ruoyi-ui audit --audit-level=moderate --include=dev`
- [local] `npm --prefix ruoyi-ui test`
- [local] `npm --prefix ruoyi-ui run build:prod`
- [local] `node --test tests/frontend-dependency-hardening.test.js`
- [local] `node --test tests/masterdata-runtime.test.js`
- [local] configured Maven `-pl ruoyi-business -am verify`
- [local] configured Maven `-pl ruoyi-business -am -Pintegration-test verify`
- [local] `npm run scan:all`
- [local] `npm run check`
- [local] `npm run close:change`
- [local] `git diff --check`

## Risks

- R-12A remains remote-CI red until this local fix is explicitly authorized for push and its distinct workflow is successful.

## Next Entry Point

Create the local implementation commit from this verified tree, then wait for explicit push authorization. Do not begin R-12B.
