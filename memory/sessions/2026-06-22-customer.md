# Session Note

## Task

`TASK-0003 / TASK-CUSTOMER` — Implement 客户管理.

## Status

`verified` with runtime closeout complete; follow-up UX/display iteration runtime-validated

## Goal

Deliver the first Door ERP customer master-data module under the locked RuoYi adapter without modifying profile, rules, scanners, workflow scripts, or governance gates.

## Changed Files

- `ruoyi-business/src/main/java/com/ruoyi/business/customer/`
- `ruoyi-admin/src/main/java/com/ruoyi/web/controller/business/customer/CustomerController.java`
- `ruoyi-business/src/main/resources/mapper/customer/CustomerMapper.xml`
- `ruoyi-ui/src/views/customer/index.vue`
- `ruoyi-ui/src/api/customer.js`
- `sql/customer.ownership.md`
- `features/customer.md`
- `ai/registry/features.json`
- `graph/api-graph.json`
- `graph/ui-graph.json`
- `memory/API_CATALOG.md`
- `ai/changes/CR-20260622T081827Z-change/`
- `ai/changes/CR-20260622T102456Z-change/`

## Commands

- `npm run resume`
- `npm run ai:do -- "新增功能：客户管理"`
- `npm run impact -- 客户管理`
- `C:\Users\11131\.cache\codex-tools\apache-maven-3.9.9\bin\mvn.cmd -pl ruoyi-admin -am -DskipTests compile`
- `npm --prefix ruoyi-ui run build:prod`
- `npm run scan:all`
- `npm run finalize:change -- --summary "新增客户管理功能"`
- `npm run check`
- `npm run ai:do -- "功能迭代：客户管理"`
- `npm run scan:all`

## Verification

`npm run check` passed, including registry, ownership, graph, memory, component, boundary, stale-doc, orphan, diff, duplicate, runtime checks, and 76 Node tests. Backend Maven compile and frontend production build also passed.

## Runtime Validation

- Applied `sql/customer.ownership.md` to local schema `my_ry_vue_runtime`.
- Started backend on `http://localhost:18080` and frontend on `http://127.0.0.1:5174`.
- Browser login succeeded with `admin/admin123`.
- Customer menu is visible under `业务管理 / 客户管理`; active route is `/business/customer`. Direct `/customer` returned 404.
- Created, edited, queried, disabled, and opened detail for `验收客户20260622092456`.
- Added two contacts and two shipping addresses; active records have exactly one default contact and one default address.
- Transferred owner from admin to ry; owner transfer log recorded `研发部门 -> 测试部门`.
- Entered long-term deposit `1000` and rolling deposit `500`; both generated fund accounts, batches, and fund flows.
- Saved sample policy `DISCOUNT_AND_REBATE` and generated sample rebate record `SAMPLE-20260622092456` with remaining amount `300`.
- Verified export endpoint returned an XLSX file saved as `ai/changes/CR-20260622T081827Z-change/runtime-evidence/customer-export.xlsx`.
- Verified normal customer edit dialog has no fund/balance fields.
- Closeout decision: keep `/business/customer` as the accepted RuoYi menu route; direct top-level customer path remains a 404 and is not a supported runtime contract.
- Closeout deposit validation created new long-term and rolling deposit entries. New fund flows `4` and `5` record `related_biz_type=CUSTOMER_DEPOSIT_BATCH`, deposit-batch ids, and `DEP...` related business numbers.
- Closeout UI validation added and verified a dedicated `样品返现记录` table in the detail page's `资金与政策` area.
- Closeout export validation clicked the UI `导出` button and captured HTTP 200 XLSX network response from `/dev-api/business/customer/export`.
- Final closeout gates passed: `npm run scan:all`, `npm run finalize:change -- --summary "客户管理运行偏差收口"`, `npm run check`, and standalone `npm test`.
- Detailed evidence: `ai/changes/CR-20260622T081827Z-change/runtime-validation.md`.

## UX / Display Follow-up

- Change record: `CR-20260622T102456Z-change`.
- Accepted runtime route remains `/business/customer`; no `/customer` route was added.
- New customer codes use `KH + yyyyMM + monthly sequence`. Browser validation created `KH202606000001` and `KH202606000002`.
- Historical `CUS...` validation records were not backfilled.
- `uk_customer_code` remains the unique constraint for `customer.customer_code`; DB duplicate count for `KH202606%` was `0`.
- Customer level `NORMAL` displays and exports as `普通`; customer type displays and exports as Chinese labels such as `经销商`.
- Add/edit customer dialogs use province/city/district cascaders. Add selected `浙江省 / 杭州市 / 滨江区`; edit echoed it and saved `浙江省 / 宁波市 / 鄞州区`.
- Blank short names on create/update save as customer name.
- Export XLSX evidence is saved as `ai/changes/CR-20260622T102456Z-change/runtime-evidence/customer-encoding-address-export.xlsx` and parsed in `export-xlsx-verification.txt`.
- Browser evidence screenshots include `01-customer-list-normal-label.png`, `02-area-cascader-filter-zhejiang.png`, `04-after-two-customers-list-kh-codes.png`, `06-edit-dialog-area-echo.png`, and `09-detail-after-edit-address.png`.
- Follow-up `npm run finalize:change -- --summary "客户管理编码字典与地址联动优化"` passed; final `npm run check` and standalone `npm test` passed with 76 Node tests.

## Region Data Follow-up

- Change record: `CR-20260622T114208Z-change`.
- Scope stays inside customer / 客户管理. No sales order, shipment, finance, profile, rules, scanner, workflow, or governance script changes.
- Root cause: `ruoyi-ui/src/views/customer/index.vue` contained a small hand-written Cascader mock; 河南省 only exposed 郑州市 and 洛阳市.
- Fix: added `ruoyi-ui/src/utils/region-data.js`, generated from `china-area-data@5.0.1 (MIT)`, and imported it into the customer page with `@/utils/region-data`.
- Coverage: 34 province-level regions; 河南省 has 18 city-level nodes, including 开封市、安阳市、新乡市、许昌市、南阳市、商丘市、周口市、驻马店市、信阳市、焦作市、濮阳市、漯河市、三门峡市、平顶山市、鹤壁市、济源示范区.
- Direct municipalities are normalized to complete 3-level paths such as `北京市 / 北京市 / 朝阳区`.
- Customer v1 still saves Chinese names only in `province`, `city`, and `district`; no `province_code`, `city_code`, or `district_code` columns were added. Future logistics/region-statistics work can add code fields through a separate DB change.
- Browser validation on `/business/customer` created `省市区完整验证客户20260622115256` with `河南省 / 开封市 / 龙亭区`, echoed it in edit, saved `北京市 / 北京市 / 朝阳区`, and confirmed list/detail/export Chinese names.
- Runtime evidence lives under `ai/changes/CR-20260622T114208Z-change/runtime-evidence/`; module check is `ai/changes/CR-20260622T114208Z-change/region-data-module-check.json`.
- Final gates passed: `npm run scan:all`, `npm run finalize:change -- --summary "客户管理省市区完整数据源修复"`, `npm --prefix ruoyi-ui run build:prod`, `npm run check`, and standalone `npm test`.

## Administrative Division Code Follow-up

- Change record: `CR-20260622T124645Z-change`.
- Scope stays inside customer / 客户管理. No sales order, shipment, finance, profile, rules, scanner, workflow, or governance script changes.
- Added nullable `province_code`, `city_code`, `district_code` to `customer` and `customer_address` in SQL ownership and local schema `my_ry_vue_runtime`.
- Updated `Customer`, `CustomerAddress`, and `CustomerMapper.xml` to receive, save, list, and detail-return the code fields.
- Regenerated `ruoyi-ui/src/utils/region-data.js` so Cascader values are administrative division codes and labels are Chinese names.
- Updated customer add/edit and shipping-address add/edit logic so payloads save both code fields and Chinese names. List/detail/export still display Chinese province/city/district names.
- Historical rows without code fields are not force-backfilled. The UI attempts name-based matching and writes codes on next confirmed save.
- Runtime browser validation used `/business/customer` on frontend `5174` and backend `18080`.
- Added customer `行政编码验证客户20260622135304` with `河南省 / 郑州市 / 中原区`; DB saved `410000 / 410100 / 410102` plus Chinese names.
- Edit dialog echoed customer area by code, then saved `广东省 / 广州市 / 天河区`; DB saved `440000 / 440100 / 440106`.
- Added shipping address `山东省 / 济南市 / 历下区`; DB saved `370000 / 370100 / 370102`, and edit echoed it by code.
- Historical no-code row `KH202606000003` opened in edit without crash and name-matched `北京市 / 北京市 / 朝阳区`.
- Detail base/address tab and export show Chinese area names only. Parsed XLSX row `KH202606000004` shows `经销商`, `普通`, `广东省`, `广州市`, `天河区`.
- Evidence includes `runtime-evidence/db-area-code-columns.txt`, `region-code-helper-check.json`, `region-code-coverage-check.json`, `db-customer-area-code-save.txt`, `db-customer-area-code-update.txt`, `db-address-area-code-save.txt`, `browser-*.png`, and `export-xlsx-area-check.json`.
- Passed: backend compile/package, local backend restart on `18080`, frontend `build:prod`, browser runtime validation, `npm run scan:all`, `npm run finalize:change -- --summary "客户管理省市区行政区划编码补齐"`, `npm run check`, and standalone `npm test`.

## Default Contact And Address Follow-up

- Change record: `CR-20260622T150304Z-change`.
- Scope stays inside customer / 客户管理. No sales order, shipment, finance, profile, rules, scanner, workflow, or governance script changes.
- Backend `CustomerServiceImpl` now prepares default child records in the same `@Transactional` create/update flow used for customer save.
- Create behavior: if no meaningful submitted contact exists and master contact/phone exists, the service creates one default contact from master contact, phone, and WeChat. If no meaningful submitted address exists and master detail address exists, the service creates one default shipping address from master contact, phone, area code/name fields, and detail address.
- Edit behavior: master data does not overwrite child records unless the request includes `syncDefaultContact=true` or `syncDefaultAddress=true`. The frontend edit dialog exposes both options in the base tab.
- Sync behavior updates only the current default child record or creates it if missing. Existing default shipping-address `logistics_line` is preserved; default uniqueness normalization ignores blank child rows and keeps one meaningful default.
- Frontend add no longer pre-populates blank contact/address rows; after save, defaults are generated by the backend and appear when the detail tabs are opened.
- Passed: `npm run resume`, `npm run ai:do -- "功能迭代：客户管理"`, backend compile, backend package after stopping the locked runtime jar, frontend `build:prod`, local runtime restart on backend `18080` plus frontend `5174`, browser/API/DB validation, `scan:all`, `finalize:change`, `check`, and standalone `npm test`.
- Runtime evidence: `default-child-api-verification-utf8.json` and `default-child-db-verification-staged-utf8.txt` created customer `默认同步UTF8验证客户20260622162301` (`KH202606000010`) and verified create auto-defaults, no-sync no-overwrite, sync update, preserved `保留线路UTF8`, and exactly one active default contact/address. Browser evidence includes `browser-03-detail-base-tab.png`; contact/address drawer-tab screenshots timed out in the in-app browser, so tab-specific evidence is stored as `browser-04-detail-contact-tab-dom.txt` and `browser-05-detail-address-tab-dom.txt`.

## Risks

- Existing pre-closeout long-term and rolling deposit flows keep their historical `related_biz_no=NULL`; new deposit flows are traceable through `CUSTOMER_DEPOSIT_BATCH` and `DEP...` related business numbers.
- Historical `CUS...` customer codes remain in runtime data and are intentionally not backfilled.
- Historical customer/address rows may still have NULL province/city/district code fields until edited and saved; this is intentional and not a runtime blocker.
- Automatic deposit/rebate deduction is deferred to later order, shipment settlement, and finance modules.

## Next Entry Point

For future customer work, keep `/business/customer` as the runtime entry and keep sales order, shipment, finance, and automatic deduction behavior in separate feature changes. If formal historical address data needs codes, create a separately reviewed backfill change.
