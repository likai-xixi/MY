# Session: RuoYi + Vue3 Base Integration

## Task

TASK-0002 — Attach a RuoYi Spring Boot backend plus RuoYi Vue3 frontend base without overwriting Codex governance directories.

## Status

`runtime-verified` — governance scans, ownership records, Maven runtime policy, and local RuoYi runtime startup/browser smoke checks have been verified.

Baseline closeout repair on 2026-06-22 also verified `npm test` no longer times out in remove-feature apply coverage, and repeated-half routes generated by older scanner/sync output have been removed from registry/UI graph outputs.

## Goal

Turn the governance template into a RuoYi-profile project base while preserving Codex workflow assets and making built-in RuoYi modules visible to registry, graph, scan, component, and handoff checks.

## Changed Files

- `pom.xml`
- `ruoyi-admin`
- `ruoyi-common`
- `ruoyi-framework`
- `ruoyi-generator`
- `ruoyi-quartz`
- `ruoyi-system`
- `ruoyi-ui`
- `sql`
- `ruoyi-ui/vite.config.js`
- `ai/project-profile.json`
- `ai/registry/features.json`
- `ai/registry/components.json`
- `graph/api-graph.json`
- `graph/ui-graph.json`
- `memory/API_CATALOG.md`
- `memory/MODULE_MAP.md`
- `memory/PROJECT_STATE.md`
- `scripts/remove-feature.js`
- `tests/remove-feature.test.js`
- `tools/ownership-syncer.js`
- `tests/ownership-syncer.test.js`

## Commands

- `npm run resume`
- `npm run start:change -- --mode profile-setup "接入 RuoYi + Vue3 项目底座"`
- `git clone --depth 1 --branch springboot3 https://gitee.com/y_project/RuoYi-Vue.git`
- `git clone --depth 1 https://github.com/yangzongzhuan/RuoYi-Vue3.git`
- `npm run profile:setup`
- `npm run profile:lock`
- `npm run scan:all`
- `node --test tests/remove-feature.test.js`
- `node --test tests/ownership-syncer.test.js`
- `npm test`
- `npm run build:graph`
- `npm run sync:memory`
- `npm run finalize:change`
- `npm run check`
- `npm run check:diff`
- `npm run scan:duplicates`
- `docker exec mj-redis redis-cli -n 1 ping`
- `docker exec mj-mysql mysql ...`
- `C:\Users\11131\.cache\codex-tools\apache-maven-3.9.9\bin\mvn.cmd clean package -DskipTests`
- backend startup on `http://localhost:18080`
- frontend startup on `http://127.0.0.1:5173`
- browser login and menu/page smoke verification

## Verification

Use `npm run check` as the governance gate. Runtime verification also passed locally:

- SQL files `sql/ry_20260417.sql` and `sql/quartz.sql` were imported into isolated database `my_ry_vue_runtime`.
- Imported database contained 31 tables, default users `admin` and `ry`, and baseline menus including `系统管理`, `系统监控`, `系统工具`, `服务监控`, and `代码生成`.
- Redis DB1 returned `PONG`.
- Backend `/captchaImage`, frontend `/`, and frontend proxy `/dev-api/captchaImage` returned HTTP 200.
- Browser verified the login page captcha, logged in, rendered the home page, opened `用户管理`, opened `服务监控`, and opened `代码生成`.
- `check:diff` and `scan:duplicates` skip nested dependency/build output directories created during runtime startup.
- `remove-feature` test fixture copying and document reference scanning skip nested `node_modules`, `target`, `dist`, `build`, `.vite`, `coverage`, and `tmp` directories at any depth.
- Ownership sync filters repeated-half route residues such as `/system/config/system/config`, `/system/user/system/user`, and `/test/user/test/user`; `npm run scan:all` refreshed `ai/registry/features.json` and `graph/ui-graph.json`.
- `node --test tests/remove-feature.test.js` passed after reproducing the pre-fix timeout.
- `npm test` passed with 76 tests.
- `npm run check` passed after `npm run scan:all`.
- Evidence is recorded in `ai/changes/CR-20260622T015319Z-ruoyi-vue3/runtime-validation.md`.

## Risks

- RuoYi SQL files contain initialization DDL/DML and must only be applied to the intended empty development database.
- Some RuoYi vendor page-local components are recorded through the active change record component exception instead of being moved.
- The current ownership graph is a governance baseline for the imported platform modules, not a proof of production readiness.
- Runtime verification used local development defaults and representative pages only; production deployment, CRUD mutations, import/export, Druid authentication, Swagger behavior, scheduled jobs, and production frontend build remain unverified.
- Frontend dependency install reported `8 vulnerabilities (3 moderate, 5 high)` via npm audit; this was recorded but not changed.

## Next Entry Point

Ask Codex to run `npm run resume`, then request the first real business feature with `新增功能：<功能名>` inside the locked RuoYi adapter roots.
