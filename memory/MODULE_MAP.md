# Module Map

Generated from `graph/module-graph.json`.

## Governance

- `agents` (`agents/`): Role contracts used by Codex App.
- `memory` (`memory/`): Persistent project state, handoff, changelog, module map, and API catalog.
- `graph` (`graph/`): Structured module, API, and UI relationship data.
- `tools` (`tools/`): Local validation and repository hygiene commands.
- `ai` (`ai/`): Machine-readable Codex registries, change records, generated scans, and rules.
- `scripts` (`scripts/`): Generated artifact helpers.

## Business

- `backend` (`backend/`): Backend business modules.
- `frontend` (`frontend/`): Frontend business modules.
- `features` (`features/`): Business feature briefs and acceptance criteria.

## Feature

- `feature:inventory` (`features/inventory.md`): Inventory List feature brief.

## Backend Module

- `backend:inventory` (`backend/modules/inventory/`): Inventory backend module.

## Frontend Module

- `frontend:inventory` (`frontend/src/modules/inventory/`): Inventory frontend module.
