# Resume Project Prompt

Read `AGENTS.md`, `memory/PROJECT_STATE.md`, `memory/TASKS.json`, `memory/HANDOVER.md`, the latest file under `memory/sessions/`, `memory/MODULE_MAP.md`, `memory/API_CATALOG.md`, `graph/module-graph.json`, `graph/api-graph.json`, `graph/ui-graph.json`, and the active feature brief.

Then report:

1. Current goal.
2. Active task and next concrete step.
3. Existing feature, backend, API, frontend, and UI boundaries.
4. Documents or graphs that may be stale.
5. Verification commands required before handoff.

Do not edit code until the current state and next step are clear.
