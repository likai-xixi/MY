# Inventory Backend

Use this folder for inventory backend boundaries.

- `api/` describes transport contracts.
- `service/` coordinates inventory use cases.
- `domain/` owns inventory business rules.
- `repository/` owns inventory persistence contracts.
