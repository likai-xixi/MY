# Backend Common

Shared backend helpers belong here only when more than one module needs them.

Avoid turning this folder into an unowned dumping ground.
See `contracts.md` for the allowed scope, dependency direction, and promotion rule.
