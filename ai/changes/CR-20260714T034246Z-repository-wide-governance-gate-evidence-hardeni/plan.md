# Plan

Mode: `rule-change`
Feature: `platform`

1. Reproduce the repository-wide false-green paths and retain `beforeSalesOrder` as blocked.
2. Replace copied RuoYi exceptions with exact canonical-hash legacy findings.
3. Bind review, Git range, changed-files, context, test ownership, verification provenance, and CI declarations to immutable repository facts.
4. Add negative regression tests for generated-looking source paths, dangerous roots, moving revisions, same-diff review approval, missing context files, Windows absolute paths, and ownership exceptions.
5. Execute scans, focused tests, the complete Node suite, dependency audit/build, Maven unit/integration verification, finalization, the main governance gate, exact-scope audit, and independent staged review.
6. Commit this governance-only batch before restoring the separately verified masterdata stash.

## Scope Boundary

- No business runtime or production application profile files.
- No sales-order, delivery, finance, production-domain, formula, model-config, or DXF runtime.
- No release or deployment.
- The platform production profile and its checker hardening remain separate ordered changes.
