# Lockfile Diff

## Manifest

- `ruoyi-ui/package.json`: no diff.
- No direct immutable dependency, override, parent version, audit setting, package manager, or workflow change.

## Exact Dependency Delta

```text
node_modules/immutable
  version: 5.1.6 -> 5.1.8
  resolved: added immutable-5.1.8.tgz registry URL
  integrity: added npm registry SHA-512 value
```

`dev=true`, license, `sass-embedded@1.97.2`, its `immutable:^5.0.2` constraint, brace-expansion 2.1.2, and every other dependency version remain unchanged.

The delta was generated by normal npm package-lock-only install/uninstall resolution, not by hand-editing lockfile metadata.
