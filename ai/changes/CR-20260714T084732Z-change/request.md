# Request

功能迭代：平台底座——补全生产环境 Druid 必需配置，并用打包后的 `prod` 启动路径证明不会再因缺少占位符而提前失败。

This record is configuration-only. It must not change governance checkers, package scripts, dependencies, business modules, sales-order code, release, or deployment.
