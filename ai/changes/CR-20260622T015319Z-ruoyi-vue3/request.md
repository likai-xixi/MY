# Request

接入 RuoYi + Vue3 项目底座。

## Requirements

1. Do not overwrite `AGENTS.md`, `ai`, `memory`, `scripts`, `tools`, `.codex`, or `docs` during source merge.
2. Pull RuoYi source into a temporary staging directory first.
3. Compare before merging into `D:\Project\MY`.
4. After merge, run `npm run profile:setup` and lock the RuoYi profile.
5. Run `npm run scan:all`, `npm run finalize:change`, and `npm run check`.
6. Report changed files, profile result, directory structure, and residual risks.
