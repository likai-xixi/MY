# Plan

1. Capture the exact four-finding audit baseline, dependency tree, 90-icon inventory, ECharts call sites, and the stale stash boundary.
2. Add platform-owned dependency checks plus executable UI regressions that run the real spritemap plugin in an in-memory Vite build, exercise the cache-chart lifecycle with controlled ECharts, resize, observer, mount, and unmount doubles, and prove stale-response, transport-error, render-error, and unmount behavior with deferred requests.
3. Upgrade ECharts to the patched major, replace the vulnerable SVG chain with the Vite-6-compatible maintained spritemap plugin and SVGO, repair the malformed button icon, remove three dead external-font style blocks, and update the one icon consumer contract.
4. Make the cache charts single-instance, resize-safe, stale-response-safe, and disposable on unmount; isolate request sequencing in a directly testable controller and preserve the existing data presentation.
5. Reinstall from the lockfile, prove zero moderate-or-higher audit findings, build production assets, inspect a safe emitted spritemap with 90/90 symbols and viewBoxes, and exercise login/icon/cache behavior in a real browser.
6. Run scanners, platform context, the full repository gate, exact-scope audit, independent staged review, and commit without release or deployment.
