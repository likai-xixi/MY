# Request

Harden frontend CI and release verification so UI tests and a moderate-or-stricter full dependency audit are explicit, make local release verification resolve Maven portably without weakening its exact integration reactor, and make executable `.mjs` helpers visible to non-route governance scanners without treating them as Vue pages.
