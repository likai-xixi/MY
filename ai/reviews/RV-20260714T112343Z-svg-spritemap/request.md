# Request

功能预审：平台前端依赖、SVG spritemap 与缓存图表生命周期修复。

The unreleased project has four moderate frontend dependency findings in ECharts and the abandoned SVG build chain. Review a bounded replacement that preserves all 90 icons, keeps existing backend/API/route/permission contracts unchanged, and makes the cache chart/request lifecycle directly testable before implementation is allowed.
