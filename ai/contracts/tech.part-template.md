# Technical Part Template Contract

Status: superseded by the R-11 generic decomposition node and process-plan version contracts.

Future part/decomposition policies are versioned artifacts referenced by a `ProcessPlanVersion`. They produce generic nodes and keyed values through the calculation boundary. Product-specific part names, dimensions, leaf roles, or segment positions must not become fixed database columns, DTO fields, Java branches, or Vue branches.
